// sideNav init
var elem = document.querySelector('.sidenav');
var instance = new M.Sidenav(elem,'draggable');

// tabs init
var el = document.querySelector('.tabs')
var instance = M.Tabs.init(el, 'swipeable');

// background work
